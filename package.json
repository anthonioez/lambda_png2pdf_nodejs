{
  "name": "png2pdf",
  "version": "1.0.0",
  "description": "Lambda function to take PNG files from a folder in S3 bucket and create a multiple page PDF and upload to another S3 bucket.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^2.6.2",
    "aws-sdk": "^2.427.0",
    "pdfkit": "^0.9.0"
  }
}
